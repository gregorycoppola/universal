# Approach 3: Constraint Logic Programming

## The Idea

Keep the Horn clause structure. Keep the calculus ratiocinator. But add a **constraint domain** — a separate solver that handles arithmetic (or strings, or finite domains, or whatever you need).